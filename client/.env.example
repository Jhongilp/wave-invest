# API server URL (WebSocket URL is automatically derived from this in production)
VITE_API_URL=http://localhost:8080

# Optional: Override WebSocket URL (defaults to derived from VITE_API_URL)
# VITE_WS_URL=ws://localhost:8080/ws
